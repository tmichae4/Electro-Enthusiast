
{{#each posts as |post|}}

{{> post-info post }}
</div>

{{else}}
<h4 class="pb-4 has-text-centered is-italic">No posts yet!</h4>
{{/each}} 
 
 
 {{!-- <nav class="navbar">
     <ul>
         <li><a href='#Phones'>Phones</a></li>
         <li><a href='#Computers'>Computers</a></li>
         <li><a href='#Tv'>Tv</a></li>
         <li><a href='#Gameconsole'>Game Consoles</a></li>
          <button id="logout" class="btn-no-style">logout</button>

     </ul>
     </nav>

<ol class="post-list">
    {{#each posts as |post|}}
    <li>
        <article class="post">
            <div class="title">
                <a href="/">{{title}}</a
                ></div>
            <div class="meta">
                {{user.username}} on {{created_at}}
                <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
            </div>
        </article>
    </li>
    {{/each}}
</ol> --}}