<ol class="post-list">
  {{#each posts as |post|}}
  <li>
    {{> post-info post }}
  </li>
  {{/each}}
</ol>




{{!-- <ol class="post-list">
  <li>
    <article class="post">
      <div class="title">
        {{title}}
        
      </div>
      <div class="meta">
        {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div>
    </article>
  </li>
</ol> --}}
 
 
 {{!-- <nav class="navbar">
     <ul>
         <li><a href='#Phones'>Phones</a></li>
         <li><a href='#Computers'>Computers</a></li>
         <li><a href='#Tv'>Tv</a></li>
         <li><a href='#Gameconsole'>Game Consoles</a></li>
          <button id="logout" class="btn-no-style">logout</button>

     </ul>
     </nav>

<ol class="post-list">
    {{#each posts as |post|}}
    <li>
        <article class="post">
            <div class="title">
                <a href="/">{{title}}</a
                ></div>
            <div class="meta">
                {{user.username}} on {{created_at}}
                <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
            </div>
        </article>
    </li>
    {{/each}}
</ol> --}}